/*  se a idade >= 18, pessoa pode ir para a festa
    senão, a pessoa não pode ir para a festa
*/
let idade = 15;

if (idade >= 18) {
    console.log("A pessoa pode ir para a festa 🎉");
} else {
    console.log("A pessoa não pode ir para a festa 🚫");
}